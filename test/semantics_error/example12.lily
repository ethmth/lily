let a:int
let b:int = 0
#let c
#let d = 5

# GCD Function (not really)
def gcd(a:int, b:int) -> int:
	def lcd(a:int, b:int) -> int:
		return 4
	
	if (a == b):
		return lcd(b, a)
	else:
		return 4

def main() -> int:
	print(lcd(4,5))
	return 0
