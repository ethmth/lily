def mergesort(l::int) -> int list:
    let new_list:: int = new int[len(l)]
    let i:int = 0
    for(i < len(l), i++):
        new_list[i] = l[i]
    return new_list


let e::int = [2,1]

e.mergesort()

print(e[0])
print(e[1])