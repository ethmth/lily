let a:int
let b:int = 0
#let c
#let d = 5

# GCD Function (not really)
def gcd(a:int, b:int) -> int:
	#print(a)
	#print(b)
	def lcd(a:int, b:int) -> int:
		return 2

	#print(a)
	#print(b)
	
	if (a == b):
		return lcd(b, a)
	else:
		return 4

def main() -> int:
	print(gcd(4,4))
	return 0

def assign() -> int:
	a = 5

main()