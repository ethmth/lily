let a:int
let b:int = 0
#let c
#let d = 5

# GCD Function (not really)
def gcd(a:int, b:int) -> int:
	def lcd(c: int, b:int) -> int:
		return a + b
	if (a == b):
		return 3
	else:
		return 4

def main() -> int:
	print(gcd(4,5))
	return 0

main()